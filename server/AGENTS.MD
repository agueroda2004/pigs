Every **method/function** has to be documented with around 2 lines using English like main language.
You don't have to document tests files. Avoid them.

---

When you create a commit, include a little description that we just did.

---

Every domain, application, infraestructure method/functions has to have a test file testing its code.

---

Every test file has to be inside its father folder like this:

    ./application
        /tests <- here they will be

**OR**

    ./infrastructure
        /tests <- here they will be

Apply this in every folder that has its own tests methods

---

If you load this file tell "Daniel I'm ready"
